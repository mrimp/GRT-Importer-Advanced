# Changelog

## 0.87.13-nextlevel
- Repo refactor: `src/` + deterministic `build/` scripts.
- Consolidated styles into `src/styles.css`.
- Consolidated runtime JS into `src/app.js`.
- `dist/index.html` remains a single-file offline release artifact.
